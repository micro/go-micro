module go-micro.dev/v4/cmd/protoc-gen-micro~

go 1.24

// Phantom path (tilde). The natural retraction tag protoc-gen-micro~/v1.18.2
// is an invalid git ref name, so this retraction is published as the root tag
// v1.18.1 instead — which is how this path is already keyed (its versions are
// the repo's v1.x root tags).
retract [v0.0.0, v1.18.0]
